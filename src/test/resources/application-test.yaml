spring:
  flyway:
    baseline-on-migrate: true
    enabled: true
    user: test
    password: test
    baseline-description: "init"
    baseline-version: 0
    locations: classpath:db/migration

  mail:
    host: 127.0.0.1
    port: 3025
    username: test
    password: test